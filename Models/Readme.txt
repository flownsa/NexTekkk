Nextekk Enterprise Application 
#####################################################################################################################
    A Simple Role-based system using Identity System from Entity Framework - EF Core 6 or Core??
#####################################################################################################################

The only variable name that is not consistent with its description is :

***  IdentityUserClaims.UserId == Employee.EmployeeId  ***


anywhere else that you may come across a variable name that is iconsistent with its description other than the exception
described above is as a result of the project still in its alpha stage. Please refer to the versions..

All references to an Employee's data key is represented by "Id" except for "OtherInfo" POCO  entities which is indexed by "EmployeeId"

